import React from 'react';

import { Container, HeaderTitle} from '../styles/MessagesStyle';

export default function Messages() {

  return(
    <Container>
      <HeaderTitle>Save-Natura</HeaderTitle>
    </Container>
  );
}