import React from 'react';

import { HeaderTitle, Container } from '../styles/ProfileStyle';

export default function Profile(){

  return(
    <Container>
      <HeaderTitle>Save-Natura</HeaderTitle>
    </Container>
  );
}